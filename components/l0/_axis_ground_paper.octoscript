// Axis delta: ground .paper — nine seeds; `_derive_color.octoscript`
// computes the palette from them in-kit. Compare `gen_accent_axes.py`,
// which had to ship 48 solved files because nothing could compute.
// Ink 16.30:1 on this ground.
let seed_on       = 1
let seed_ground_r = 250
let seed_ground_g = 250
let seed_ground_b = 250
let seed_ink_r    = 28
let seed_ink_g    = 28
let seed_ink_b    = 30
let seed_accent_r = 61
let seed_accent_g = 72
let seed_accent_b = 184
