// Axis delta: ground .teal — nine seeds; `_derive_color.octoscript`
// computes the palette from them in-kit. Compare `gen_accent_axes.py`,
// which had to ship 48 solved files because nothing could compute.
// Ink 7.57:1 on this ground.
let seed_on       = 1
let seed_ground_r = 13
let seed_ground_g = 59
let seed_ground_b = 52
let seed_ink_r    = 232
let seed_ink_g    = 199
let seed_ink_b    = 106
let seed_accent_r = 212
let seed_accent_g = 168
let seed_accent_b = 60
