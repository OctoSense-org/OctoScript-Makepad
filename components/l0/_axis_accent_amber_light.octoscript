// Axis delta: accent .amber over `light` — generated by lab/gates/gen_accent_axes.py.
//
// The hue reaches the INK, which is what a palette is; the ground stays
// the mood's. Tint 50% toward the hue — the strongest pull that
// still clears 4.5:1 on BOTH the page and the composited panel.
// Worst pair 4.54:1  ·  bar/chip fill below 3:1 on this mood

let l0_text     = argb(255, 136,  97,  32)
let l0_soft     = argb(255, 148, 112,  54)
let l0_dim      = argb(255, 147, 112,  53)

let l0_accent   = argb(255, 245, 166,  35)
let l0_bar      = l0_accent
let l0_bar_rail = argb( 38, 128, 128, 128)
let l0_active   = argb( 72, 245, 166, 35)
let l0_go       = l0_accent

// accent-as-seed: with a seeded ground, _derive_color re-solves the
// ink against THAT ground in-kit (unrolled five-step contrast search)
// instead of keeping this fragment's mood-solved literals.
let seed_hue_on = 1
let seed_hue_r  = 245
let seed_hue_g  = 166
let seed_hue_b  = 35
