// Axis delta: ground .ivory — nine seeds; `_derive_color.splash`
// computes the palette from them in-kit. Compare `gen_accent_axes.py`,
// which had to ship 48 solved files because nothing could compute.
// Ink 13.60:1 on this ground.
let seed_on       = 1
let seed_ground_r = 246
let seed_ground_g = 241
let seed_ground_b = 231
let seed_ink_r    = 42
let seed_ink_g    = 36
let seed_ink_b    = 32
let seed_accent_r = 154
let seed_accent_g = 120
let seed_accent_b = 23
