// Axis delta: ground .sand — nine seeds; `_derive_color.octoscript`
// computes the palette from them in-kit. Compare `gen_accent_axes.py`,
// which had to ship 48 solved files because nothing could compute.
// Ink 9.32:1 on this ground.
let seed_on       = 1
let seed_ground_r = 232
let seed_ground_g = 220
let seed_ground_b = 200
let seed_ink_r    = 58
let seed_ink_g    = 50
let seed_ink_b    = 38
let seed_accent_r = 176
let seed_accent_g = 95
let seed_accent_b = 44
