// Axis delta: ground .midnight — nine seeds; `_derive_color.octoscript`
// computes the palette from them in-kit. Compare `gen_accent_axes.py`,
// which had to ship 48 solved files because nothing could compute.
// Ink 14.96:1 on this ground.
let seed_on       = 1
let seed_ground_r = 10
let seed_ground_g = 16
let seed_ground_b = 48
let seed_ink_r    = 223
let seed_ink_g    = 230
let seed_ink_b    = 255
let seed_accent_r = 109
let seed_accent_g = 121
let seed_accent_b = 240
